# Полная версия с поддержкой локальной модели Llama 3
# ВНИМАНИЕ: Требуется мощный ПК (16GB+ RAM, желательно GPU с CUDA)

# Базовые зависимости
streamlit>=1.28.0
python-dotenv>=1.0.0
langchain>=0.1.0
langchain-community>=0.0.10
langchain-core>=0.1.10
openai>=1.3.0

# Для локальной модели
transformers>=4.35.0
accelerate>=0.25.0
sentence-transformers>=2.2.2
torch>=2.1.0
huggingface-hub>=0.16.4,<0.18

# Векторные БД (обе варианта)
faiss-cpu>=1.7.4
chromadb>=0.4.0

# Обработка документов
unstructured[pdf]>=0.11.0
requests>=2.31.0

# Квантизация (опционально, работает только на Linux/CUDA)
# Раскомментируйте следующую строку если у вас Linux с CUDA:
# bitsandbytes>=0.41.0

# Для Windows без CUDA используйте обычную загрузку модели (медленнее, но работает)

